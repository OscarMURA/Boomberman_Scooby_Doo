package com.example.bomberscoobydoo.model;

// The code snippet is defining an enumeration called `MoveType`. An enumeration is a special data type
// that allows a variable to be a set of predefined constants. In this case, the `MoveType` enumeration
// has five constants: `UP`, `DOWN`, `LEFT`, `RIGHT`, and `STOP`. These constants represent different
// types of movements, such as moving up, down, left, right, or stopping.
public enum MoveType {
    UP, DOWN, LEFT, RIGHT, STOP;
}
