package com.example.bomberscoobydoo.model;

// The code snippet is defining an enumeration called `Destructible`. An enumeration is a special type
// in Java that represents a fixed set of constants. In this case, the `Destructible` enumeration has
// two constants: `INDESTRUCTIBLE` and `DESTRUCTIBLE`. These constants can be used to represent the
// destructibility of an object in a game or any other scenario where destructibility is relevant.
public enum Destructible {
    INDESTRUCTIBLE,
    DESTRUCTIBLE,
}
